---
// HeroSection Astro コンポーネント - Modern Design
---

<section class="relative min-h-screen flex items-center hero-gradient overflow-hidden">
  <!-- Background decorative elements -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute -top-32 -right-32 w-128 h-128 bg-primary/20 rounded-full blur-3xl animate-float"></div>
    <div class="absolute -bottom-32 -left-32 w-128 h-128 bg-secondary/20 rounded-full blur-3xl animate-float" style="animation-delay: 2s;"></div>
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-gradient-to-r from-primary/10 to-secondary/10 rounded-full blur-3xl animate-pulse-glow"></div>
  </div>
  
  <div class="container-custom relative z-10">
    <div class="max-w-5xl mx-auto text-center">
      <div class="animate-fade-in">
        <h1 class="mb-8 leading-tight tracking-tight">
          外国人研究参加者集め、<br />
          <span class="text-gradient">
            もう悩まない！
          </span><br />
          <span class="text-2xl md:text-4xl lg:text-5xl font-medium text-muted-foreground">AIがあなたの研究を加速します</span>
        </h1>
        
        <p class="text-lg md:text-xl lg:text-2xl text-muted-foreground mb-12 max-w-4xl mx-auto leading-relaxed font-light">
          研究者が本来の研究活動に集中できるよう、外国人参加者リクルート、研究関連文書の翻訳、関連事務作業を、
          <span class="text-primary font-semibold">AIと専門チームの力で包括的にサポート</span>します。
        </p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center mb-16">
          <button 
            id="contact-btn"
            class="btn-primary animate-pulse-glow"
          >
            まずは無料相談・資料請求
          </button>
          <button 
            id="services-btn"
            class="btn-outline"
          >
            サービス詳細を見る
          </button>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
          <div class="card-modern p-8 animate-slide-in-left">
            <div class="text-4xl font-bold text-primary mb-3">15年+</div>
            <div class="text-muted-foreground font-medium">外国人サポート実績</div>
          </div>
          <div class="card-modern p-8 animate-slide-in-left" style="animation-delay: 0.2s;">
            <div class="text-4xl font-bold text-secondary mb-3">AI活用</div>
            <div class="text-muted-foreground font-medium">効率的マッチング</div>
          </div>
          <div class="card-modern p-8 animate-slide-in-left" style="animation-delay: 0.4s;">
            <div class="text-4xl font-bold text-gradient mb-3">24時間</div>
            <div class="text-muted-foreground font-medium">迅速対応体制</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // スムーススクロール機能
  document.getElementById('contact-btn')?.addEventListener('click', () => {
    const element = document.getElementById('contact');
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  });

  document.getElementById('services-btn')?.addEventListener('click', () => {
    const element = document.getElementById('services');
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  });
</script>