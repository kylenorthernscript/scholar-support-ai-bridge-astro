---
// HeroSection Astro コンポーネント - Modern Design
---

<section class="relative min-h-screen flex items-center bg-slate-50 dark:bg-slate-900 overflow-hidden">
  <!-- Clean background -->
  <div class="absolute inset-0 overflow-hidden">
    <!-- Subtle grid pattern for texture -->
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_1px_1px,hsl(var(--muted))_1px,transparent_0)] bg-[size:32px_32px] opacity-20"></div>
  </div>
  
  <div class="container-custom relative z-10">
    <div class="max-w-5xl mx-auto text-center">
      <div class="animate-fade-in pt-20 md:pt-0">
        <h1 class="hero-title mb-12 max-w-4xl mx-auto">
          <span class="block text-3xl md:text-4xl lg:text-5xl font-bold text-foreground mb-8 tracking-tight leading-tight">
            研究者のための外国人参加者リクルート<br class="md:hidden" />→ 研究時間を本来の目的へ
          </span>
          <span class="block text-xl md:text-2xl lg:text-3xl font-medium text-muted-foreground tracking-normal leading-relaxed">
            研究予算を無駄にしない効率的なリクルートで、あなたの貴重な研究時間を研究そのものに集中させます
          </span>
        </h1>
        
        <p class="text-lg md:text-xl lg:text-2xl text-muted-foreground mb-12 max-w-4xl mx-auto leading-relaxed font-light">
          <span class="text-primary font-semibold">シータ・クリニカル・サポート</span>は、研究者が本来の研究活動に集中できるよう、外国人参加者リクルート、研究関連文書の翻訳、関連事務作業を、<span class="text-primary font-semibold">AIと専門チームの力で包括的にサポート</span>します。
        </p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center mb-16">
          <button 
            id="contact-btn"
            class="btn-primary animate-pulse-glow"
          >
            研究計画に合わせた無料見積もり
          </button>
          <button 
            id="services-btn"
            class="btn-outline"
          >
            サービス詳細を見る
          </button>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
          <div class="card-modern p-8 animate-slide-in-left">
            <div class="text-4xl font-bold text-primary mb-3">15年+</div>
            <div class="text-muted-foreground font-medium">外国人サポート実績</div>
          </div>
          <div class="card-modern p-8 animate-slide-in-left" style="animation-delay: 0.2s;">
            <div class="text-4xl font-bold text-secondary mb-3">AI活用</div>
            <div class="text-muted-foreground font-medium">効率的マッチング</div>
          </div>
          <div class="card-modern p-8 animate-slide-in-left" style="animation-delay: 0.4s;">
            <div class="text-4xl font-bold text-gradient mb-3">24時間</div>
            <div class="text-muted-foreground font-medium">迅速対応体制</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // スムーススクロール機能
  document.getElementById('contact-btn')?.addEventListener('click', () => {
    const element = document.getElementById('contact');
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  });

  document.getElementById('services-btn')?.addEventListener('click', () => {
    const element = document.getElementById('services');
    if (element) {
      element.scrollIntoView({ behavior: 'smooth' });
    }
  });
</script>